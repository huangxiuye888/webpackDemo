import './index.less'
const arr = [1, 2, 3];
const newArr = arr.map(item => item + 1);
const newnum = num + 123
const node=document.createElement('div')
node.textContent="Hello World"
document.body.appendChild(node)
